In first time you need to do a update database,
write it in Package Manager Console:

update-database -context ApplicationDbContext

update-database -context AppIdentityDbContext

For change seed data in Identity database go to AppIdentityDbContext.cs

Admin Account: admin@mail.com

Admin password: 111111Test
